<?xml version="1.0" encoding="utf-8"?>
<!--
  Light-mode Android themes for Secretly.
  IMPORTANT: do NOT switch the NormalTheme parent to `Theme.Black.NoTitleBar`
  or hard-code `windowBackground` to `@android:color/black`. Doing so makes
  the OS paint an opaque black band where the system navigation bar lives,
  defeating the edge-to-edge transparent-bar setup in `main.dart` and
  hiding the `SystemBottomFadeLayer` gradient. Keep `?android:colorBackground`
  so the dynamic theme tint can show through.
-->
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Transparent: IncomingCallActivity is now a wake-shim that finishes
         immediately without drawing UI (the CallStyle notification is the UI),
         so it must never paint a black/opaque window on the lockscreen. -->
    <style name="IncomingCallTheme" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
